import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pfro.app',
  appName: 'Pastoral Files Record Office (PFRO)',
  webDir: 'dist'
};

export default config;
